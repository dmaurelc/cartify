{
  "name": "pdr-portal-digital-restaurantes",
  "version": "0.1.0",
  "description": "Portal Digital de Restaurantes - SaaS platform for restaurant digital menus and orders",
  "private": true,
  "keywords": [
    "restaurant",
    "saas",
    "nextjs",
    "nestjs",
    "typescript",
    "digital-menu",
    "qr-code"
  ],
  "author": "PDR Team",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=src/frontend & npm run dev --workspace=src/backend",
    "build": "npm run build --workspace=src/frontend && npm run build --workspace=src/backend",
    "start": "npm run start --workspace=src/backend",
    "test": "npm run test --workspace=src/frontend && npm run test --workspace=src/backend",
    "test:watch": "npm run test:watch --workspace=src/frontend & npm run test:watch --workspace=src/backend",
    "test:coverage": "npm run test:coverage --workspace=src/frontend && npm run test:coverage --workspace=src/backend",
    "lint": "npm run lint --workspace=src/frontend && npm run lint --workspace=src/backend",
    "format": "npm run format --workspace=src/frontend && npm run format --workspace=src/backend",
    "type-check": "npm run type-check --workspace=src/frontend && npm run type-check --workspace=src/backend",
    "docker:build": "docker-compose -f docker/docker-compose.yml build",
    "docker:up": "docker-compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker-compose -f docker/docker-compose.yml down",
    "docker:logs": "docker-compose -f docker/docker-compose.yml logs -f",
    "db:migrate": "npm run db:migrate --workspace=src/backend",
    "db:seed": "npm run db:seed --workspace=src/backend",
    "db:reset": "npm run db:reset --workspace=src/backend"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.54.0",
    "prettier": "^3.1.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {}
}
